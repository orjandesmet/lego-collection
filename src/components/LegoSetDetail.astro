---
import { type CollectionEntry, getCollection } from 'astro:content';
import LegoSetInfoBlock from './LegoSetInfoBlock.astro';
import LegoSetHeader from './LegoSetHeader.astro';
import LegoSetImage from './LegoSetImage.astro';

type Props = {
  collection: 'sets' | 'minifigs';
  legoSetData: CollectionEntry<'sets' | 'minifigs'>['data'];
};
const { collection, legoSetData } = Astro.props;

function stringifyBuildTime(
  totalTimeSpent: Props['legoSetData']['totalTimeSpent']
): string {
  if (!totalTimeSpent) {
    return '';
  }
  let buildTime = `${totalTimeSpent.minutes}m`;
  if (totalTimeSpent.hours) {
    buildTime = `${totalTimeSpent.hours}h ${buildTime}`;
  }
  return buildTime;
}
const themes = await getCollection('themes');

const buildTime = stringifyBuildTime(legoSetData.totalTimeSpent);
const [setNumber, variant] = legoSetData.setNumber.split('-');
const taxonomy = legoSetData.themes
  .map((theme) => themes.find((t) => t.id === theme.id)?.data.name)
  .join(' - ');
const isWishlist = legoSetData.wishlist ?? false;
const showVariant = collection === 'minifigs';
---

<article class="bg-white dark:bg-transparent">
  <LegoSetHeader
    title={legoSetData.name}
    taxonomy={taxonomy}
    wishlist={isWishlist}
    backButton=`/${collection}`
  />
  <LegoSetImage img={legoSetData.img} alt={legoSetData.name} />
  <section class="py-4">
    <div class="w-fit mx-auto flex items-end gap-8">
      <LegoSetInfoBlock
        icon="pound"
        title={collection === 'sets' ? 'Set' : 'minifig N°'}
        >{showVariant ? `${setNumber}-${variant}` : setNumber}</LegoSetInfoBlock
      >
      <LegoSetInfoBlock icon="brick" title="Pieces"
        >{legoSetData.parts}</LegoSetInfoBlock
      >
      <LegoSetInfoBlock icon="calendar" title="Year"
        >{legoSetData.year}</LegoSetInfoBlock
      >
      {
        buildTime && (
          <LegoSetInfoBlock icon="stopwatch" title="Build time">
            {buildTime}
          </LegoSetInfoBlock>
        )
      }
      <a
        href={`https://www.lego.com/nl-be/service/building-instructions/${setNumber}`}
        target="_blank"
        rel="noopener noreferrer"
      >
        <LegoSetInfoBlock icon="book-open" title=""
          >Instructions <svg
            xmlns="http://www.w3.org/2000/svg"
            class="inline-block"
            viewBox="0 0 24 24"
            height="1rem"
            width="1rem"><use href="/external.svg#external"></use></svg
          ></LegoSetInfoBlock
        >
      </a>
    </div>
  </section>
</article>

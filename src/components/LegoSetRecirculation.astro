---
import type { CollectionEntry } from 'astro:content';
import LegoSetRecirculationItem from './LegoSetRecirculationItem.astro';
type Props = {
  collection: 'sets' | 'minifigs';
  title: string;
  sets: Array<CollectionEntry<'sets' | 'minifigs'>>;
  showVariant?: boolean;
};

const { collection, title, sets, showVariant } = Astro.props;
const containerAccentClass =
  collection === 'sets'
    ? 'border-cyan-200/80 shadow-cyan-900/10 dark:border-cyan-300/35 dark:shadow-cyan-950/20'
    : 'border-lime-200/80 shadow-lime-900/10 dark:border-lime-300/35 dark:shadow-lime-950/20';
const titleAccentClass =
  collection === 'sets'
    ? 'text-cyan-900 dark:text-cyan-200'
    : 'text-lime-900 dark:text-lime-200';
---

<aside
  class:list={[
    'rounded-3xl border bg-white/70 p-4 shadow-md backdrop-blur-sm dark:bg-slate-900/60 dark:shadow-slate-950/35 md:p-6',
    containerAccentClass,
  ]}
>
  <div class="mx-auto flex max-w-6xl flex-col gap-3 md:gap-4">
    <h3 class:list={['text-lg font-semibold md:text-xl', titleAccentClass]}>
      {title}
    </h3>
    <section class="-mx-4 overflow-x-auto md:mx-0">
      <ul class="flex w-max items-stretch gap-3 px-4 md:gap-4 md:px-0">
        {
          sets.map((set) => (
            <li class="flex">
              <LegoSetRecirculationItem
                collection={collection}
                legoSetData={set.data}
                setId={set.id}
                showVariant={showVariant}
              />
            </li>
          ))
        }
      </ul>
    </section>
  </div>
</aside>
